#include<string>
#include<vector>

namespace nsolver{

//Solve an n-game (e.g. 24-game) using vector of numbers v and return string to show answer
//For now, just put in 4 numbers into v
std::string SolveBT(int n, std::vector<int> v);

}
